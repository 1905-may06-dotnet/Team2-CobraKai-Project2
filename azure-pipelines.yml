# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'
  pathtosolution: 'Project2Solution'
  pathtoproject: 'Project2Solution/Project.Client'
  pathtotest: 'Proejct2Solution/Project.Test'

steps:

#to add: sonar cloud prepare analysis

- script: dotnet build --configuration $(buildConfiguration)
  displayName: 'dotnet build $(buildConfiguration)'
  workingDirectory: $(pathtosolution)

- script: dotnet test 
    --configuration $(buildConfiguration)
    --no-build
  workingDirectory: $(pathtosolution)
  displayName: 'dotnet test'

#- task: DotNetCoreCLI@2
#  inputs:
#      command: 'publish'
#  publishWebProjects: true

#to add: sonar cloud analyze and 

#- task: AzureRmWebAppDeployment@4
#  inputs:
#    ConnectionType: 'AzureRM'
#    azureSubscription: 'connectiondc'
#    appType: 'webApp'
#    WebAppName: '' #to determine web service name
#    packageForLinux: '$(System.DefaultWorkingDirectory)/$(pathtoproject)/**/*.zip'

- task: DotNetCoreCLI@2
  inputs:
   command: publish
   publishWebProjects: True
   arguments: '--configuration $(BuildConfiguration) --output $(Build.ArtifactStagingDirectory)'
   zipAfterPublish: True

- task: PublishBuildArtifacts@1